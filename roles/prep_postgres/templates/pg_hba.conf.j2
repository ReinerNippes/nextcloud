# !!! This file managed by Ansible. Any local changes may be overwritten. !!!

# Database administrative login by UNIX sockets
# note: you may wish to restrict this further later
local   all         postgres                          peer
local   {{ nc_db }}   {{ web_user[ansible_distribution] }}                             peer

# TYPE  DATABASE    USER        CIDR-ADDRESS          METHOD
local   all         all                               md5
host    all         all         {{ postgres_allowed_ipv4 }} md5
host    all         all         {{ postgres_allowed_ipv6 }} md5
