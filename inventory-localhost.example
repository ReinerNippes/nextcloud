---
all:
  vars:
    ansible_connection: local

nextcloud:
  hosts:
    localhost:

redis:
  hosts:
    localhost:

database:
  hosts:
    localhost:

webserver:
  hosts:
    localhost:

elasticsearch:
  hosts:
    localhost:

docker:
  hosts:
    localhost:

# nextcloudoffice and onlyoffice are mutual exclusive
# choose one or the other
nextcloudoffice:
  hosts:
    localhost:

#onlyoffice:
#  hosts:
#    localhost:

whiteboard:
  hosts:
    localhost:
